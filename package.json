{
  "name": "gentlercoparent",
  "version": "1.0.0",
  "description": "A co-parenting app website to improve communication and manage schedules.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:analytics": "esbuild js/analytics.js --bundle --outfile=js/analytics.bundle.js --format=iife --platform=browser"
  },
  "keywords": ["co-parenting", "communication", "scheduling"],
  "author": "Phillip Holland",
  "license": "ISC",
  "dependencies": {
    "@vercel/analytics": "^1.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
